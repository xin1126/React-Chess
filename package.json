{
  "name": "react-chatroom-server",
  "version": "1.0.0",
  "main": "server.js",
  "dependencies": {
    "express": "^4.15.3"
  },
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm install && npm run build"
  }
}